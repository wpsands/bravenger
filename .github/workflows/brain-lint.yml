name: Brain Lint

on:
  pull_request:
    paths:
      - 'company-brain/**'
      - 'prompts/**'
      - 'scripts/**'
  push:
    branches: [main]
    paths:
      - 'company-brain/**'
      - 'prompts/**'
      - 'scripts/**'

jobs:
  lint:
    name: Validate Brain
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Run Brain lint
        run: node scripts/lint-brain.js
